# --- Secrets / environment ---
.env
.env.*
*.pem
*.key

# --- Databases generated at runtime by the notebook ---
# (checkpoints.sqlite = LangGraph SqliteSaver checkpointer)
# (solar_farm.db = reports/approval database)
*.sqlite
*.sqlite3
*.db

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# --- Virtual environments ---
venv/
.venv/
env/
ENV/

# --- Jupyter ---
.ipynb_checkpoints/

# --- Docker ---
# (Dockerfile, docker-compose.yml, requirements.txt, api.py are
#  deliverables generated by the notebook — keep those committed,
#  do NOT ignore them)

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vscode/
.idea/
